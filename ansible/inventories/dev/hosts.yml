---
all:
  children:
    etcd:
      hosts:
        etcd-1:
          ansible_host: 10.0.1.10
          etcd_name: etcd-1
        etcd-2:
          ansible_host: 10.0.1.11
          etcd_name: etcd-2
        etcd-3:
          ansible_host: 10.0.1.12
          etcd_name: etcd-3

    servers:
      hosts:
        server-1:
          ansible_host: 10.0.1.20
        server-2:
          ansible_host: 10.0.1.21

    agents:
      hosts:
        agent-1:
          ansible_host: 10.0.2.10
          agent_zone: zone-a
        agent-2:
          ansible_host: 10.0.2.11
          agent_zone: zone-b
        agent-3:
          ansible_host: 10.0.2.12
          agent_zone: zone-c

    windows_guests:
      hosts:
        # Windows VMs for driver installation
        # win-vm-1:
        #   ansible_host: 10.0.3.100
        #   ansible_connection: winrm
        #   ansible_winrm_transport: ntlm

  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/hypervisor-dev.pem
